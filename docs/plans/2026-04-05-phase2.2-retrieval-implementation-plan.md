# Phase 2.2: Retrieval/Search Mode — Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Add a TF-IDF retrieval policy that ranks documents by query relevance, demonstrating that the same BasePolicy interface, evaluation metrics, and CI regression gates generalize across decision domains.

**Architecture:** `RetrievalPolicy` extends `BasePolicy`. `fit()` builds a TF-IDF matrix from a small document corpus. `score()` ranks documents by cosine similarity to a query passed via `context={"query": "..."}`. The same NDCG/MRR/HitRate metrics evaluate retrieval quality. A graded-relevance corpus with vocabulary overlap produces realistic scores (0.6-0.9 range).

**Tech Stack:** Python 3.11 / scikit-learn (TfidfVectorizer) / Polars / FastAPI / Pydantic

**IMPORTANT:** The portfolio story is "same harness, different domain" — the retrieval implementation is the least important part. The corpus design and evaluation integration matter most.

---

## Task 1: Retrieval Corpus Fixture

Create a small corpus with graded relevance and vocabulary overlap for meaningful NDCG scores.

**Files:**
- Create: `tests/fixtures/retrieval_corpus.json`

**Step 1: Create the corpus**

Create `tests/fixtures/retrieval_corpus.json`:

```json
{
  "documents": [
    {"id": 1, "title": "Introduction to Machine Learning", "text": "Machine learning is a subset of artificial intelligence that enables systems to learn from data. Supervised learning uses labeled examples to train models for classification and regression tasks."},
    {"id": 2, "title": "Deep Learning with Neural Networks", "text": "Deep learning uses multi-layer neural networks to learn hierarchical representations. Convolutional networks excel at image recognition while recurrent networks handle sequential data."},
    {"id": 3, "title": "Natural Language Processing Fundamentals", "text": "NLP combines linguistics and machine learning to process human language. Key tasks include sentiment analysis, named entity recognition, and machine translation."},
    {"id": 4, "title": "Reinforcement Learning Basics", "text": "Reinforcement learning trains agents to make sequential decisions by maximizing cumulative reward. Key concepts include policies, value functions, and exploration vs exploitation."},
    {"id": 5, "title": "Statistical Learning Theory", "text": "Statistical learning theory provides the mathematical foundation for machine learning. Topics include bias-variance tradeoff, VC dimension, and generalization bounds."},
    {"id": 6, "title": "Feature Engineering for ML Models", "text": "Feature engineering transforms raw data into informative inputs for machine learning models. Techniques include one-hot encoding, normalization, and polynomial features."},
    {"id": 7, "title": "Model Evaluation and Validation", "text": "Proper evaluation prevents overfitting and ensures model generalization. Cross-validation, holdout sets, and metrics like precision, recall, and F1-score are essential tools."},
    {"id": 8, "title": "Recommendation Systems Overview", "text": "Recommendation systems predict user preferences for items. Collaborative filtering uses user-item interaction patterns while content-based methods use item features."},
    {"id": 9, "title": "Time Series Forecasting Methods", "text": "Time series forecasting predicts future values from historical sequences. Methods range from ARIMA and exponential smoothing to modern approaches using LSTMs and transformers."},
    {"id": 10, "title": "Computer Vision Applications", "text": "Computer vision enables machines to interpret visual information. Applications include object detection, image segmentation, facial recognition, and autonomous driving."},
    {"id": 11, "title": "Bayesian Methods in ML", "text": "Bayesian approaches quantify uncertainty in machine learning predictions. Bayesian optimization is used for hyperparameter tuning, and Gaussian processes provide probabilistic regression."},
    {"id": 12, "title": "Gradient Boosting Machines", "text": "Gradient boosting builds ensembles of weak learners sequentially. XGBoost and LightGBM are popular implementations that excel on tabular data with fast training and high accuracy."},
    {"id": 13, "title": "Transfer Learning Strategies", "text": "Transfer learning reuses knowledge from pre-trained models on new tasks. Fine-tuning large language models and using pre-trained image classifiers as feature extractors are common strategies."},
    {"id": 14, "title": "Clustering and Unsupervised Learning", "text": "Unsupervised learning discovers patterns without labels. K-means clustering, DBSCAN, and hierarchical clustering group similar data points. Dimensionality reduction with PCA reveals structure."},
    {"id": 15, "title": "Ethics and Fairness in AI", "text": "AI fairness ensures models do not discriminate against protected groups. Bias can enter through training data, feature selection, or objective function design. Auditing and debiasing are active research areas."},
    {"id": 16, "title": "ML Infrastructure and MLOps", "text": "MLOps applies DevOps practices to machine learning. Feature stores, model registries, experiment tracking, and CI/CD for ML pipelines enable reliable model deployment."},
    {"id": 17, "title": "Anomaly Detection Techniques", "text": "Anomaly detection identifies unusual patterns in data. Techniques include isolation forests, autoencoders, and statistical methods like z-score thresholding on time-windowed data."},
    {"id": 18, "title": "Causal Inference for Decision Making", "text": "Causal inference moves beyond correlation to understand cause and effect. Techniques include randomized experiments, instrumental variables, and inverse propensity scoring for observational data."},
    {"id": 19, "title": "Optimization Methods for ML", "text": "Optimization drives model training. Stochastic gradient descent and its variants (Adam, RMSprop) minimize loss functions. Constrained optimization handles fairness and resource constraints."},
    {"id": 20, "title": "Data Pipeline Architecture", "text": "Data pipelines transform raw data into ML-ready features. Batch processing with Spark and stream processing with Kafka handle different latency requirements. Schema validation prevents data quality issues."},
    {"id": 21, "title": "A/B Testing and Experimentation", "text": "A/B testing compares treatment and control groups to measure causal effects. Statistical significance, confidence intervals, and minimum detectable effects guide experiment design and analysis."},
    {"id": 22, "title": "Ensemble Methods Beyond Boosting", "text": "Ensemble methods combine multiple models for better predictions. Random forests use bagging with decision trees. Stacking trains a meta-learner on base model outputs."},
    {"id": 23, "title": "Dimensionality Reduction Techniques", "text": "Dimensionality reduction compresses high-dimensional data. PCA finds linear projections maximizing variance. t-SNE and UMAP create non-linear embeddings for visualization."},
    {"id": 24, "title": "Online Learning and Bandits", "text": "Online learning updates models incrementally as data arrives. Multi-armed bandits balance exploration and exploitation. Contextual bandits use features to personalize decisions in real time."},
    {"id": 25, "title": "Graph Neural Networks", "text": "Graph neural networks operate on graph-structured data. Node classification, link prediction, and graph classification are key tasks. Applications include social networks and molecular property prediction."},
    {"id": 26, "title": "Hyperparameter Optimization", "text": "Hyperparameter tuning finds optimal model configurations. Grid search is exhaustive but expensive. Random search and Bayesian optimization offer better efficiency for high-dimensional search spaces."},
    {"id": 27, "title": "Deploying ML Models in Production", "text": "Production ML requires model serving, monitoring, and versioning. REST APIs, batch inference, and edge deployment serve different latency needs. Feature stores ensure training-serving consistency."},
    {"id": 28, "title": "Text Classification Methods", "text": "Text classification assigns labels to documents. Traditional methods use TF-IDF features with logistic regression. Modern approaches fine-tune transformer models like BERT for superior accuracy."},
    {"id": 29, "title": "Generative Models and GANs", "text": "Generative models learn data distributions to create new samples. GANs use adversarial training between generator and discriminator. Variational autoencoders provide a probabilistic alternative."},
    {"id": 30, "title": "Multi-Task and Meta-Learning", "text": "Multi-task learning shares representations across related tasks for better generalization. Meta-learning trains models that adapt quickly to new tasks with few examples, also called learning to learn."}
  ],
  "queries": [
    {
      "id": "q1",
      "text": "machine learning fundamentals and supervised methods",
      "relevant": {"1": 3, "5": 2, "6": 1, "7": 1},
      "description": "Broad query — doc 1 is primary, 5/6/7 are partially relevant"
    },
    {
      "id": "q2",
      "text": "neural networks and deep learning architectures",
      "relevant": {"2": 3, "25": 1, "13": 2},
      "description": "Doc 2 is primary, 13 (transfer learning) and 25 (GNNs) share vocabulary"
    },
    {
      "id": "q3",
      "text": "reinforcement learning exploration exploitation tradeoff",
      "relevant": {"4": 3, "24": 3, "11": 1},
      "description": "Two strongly relevant docs (4 and 24), Bayesian (11) shares optimization concepts"
    },
    {
      "id": "q4",
      "text": "recommendation systems and collaborative filtering",
      "relevant": {"8": 3, "14": 1},
      "description": "Doc 8 is directly relevant, 14 (clustering) is tangentially related"
    },
    {
      "id": "q5",
      "text": "model evaluation cross validation metrics",
      "relevant": {"7": 3, "21": 2, "26": 1},
      "description": "Doc 7 is primary, A/B testing (21) shares evaluation concepts, hyperparams (26) touches metrics"
    },
    {
      "id": "q6",
      "text": "production ML deployment infrastructure MLOps",
      "relevant": {"16": 3, "27": 3, "20": 2},
      "description": "Two primary docs, data pipelines (20) is supporting"
    },
    {
      "id": "q7",
      "text": "causal inference and counterfactual evaluation",
      "relevant": {"18": 3, "21": 2, "4": 1},
      "description": "Doc 18 is primary, A/B testing (21) shares causal concepts"
    },
    {
      "id": "q8",
      "text": "anomaly detection statistical methods",
      "relevant": {"17": 3, "11": 1, "14": 1},
      "description": "Doc 17 is primary, Bayesian (11) and clustering (14) share statistical methods"
    },
    {
      "id": "q9",
      "text": "gradient boosting XGBoost LightGBM tabular data",
      "relevant": {"12": 3, "22": 2, "6": 1},
      "description": "Doc 12 is primary, ensembles (22) is related, feature engineering (6) supports"
    },
    {
      "id": "q10",
      "text": "text classification NLP language models",
      "relevant": {"28": 3, "3": 3, "13": 2},
      "description": "Two primary docs, transfer learning (13) is related via fine-tuning"
    },
    {
      "id": "q11",
      "text": "fairness bias AI ethics responsible",
      "relevant": {"15": 3, "19": 1},
      "description": "Doc 15 is primary, optimization (19) touches constrained fairness"
    },
    {
      "id": "q12",
      "text": "time series forecasting prediction sequences",
      "relevant": {"9": 3, "2": 1},
      "description": "Doc 9 is primary, deep learning (2) mentions sequential data"
    }
  ]
}
```

**Notes on corpus design:**
- 30 documents, 12 queries
- Graded relevance: 3 = highly relevant, 2 = moderately relevant, 1 = marginally relevant
- Vocabulary overlap designed so multiple documents match each query with varying relevance
- Expected NDCG@10 range: 0.6-0.9 (not trivially 1.0)

**Step 2: Commit**

```bash
git add tests/fixtures/retrieval_corpus.json
git commit -m "feat: add retrieval corpus with graded relevance for search evaluation"
```

---

## Task 2: RetrievalPolicy — TF-IDF Scoring + Tests

**Files:**
- Create: `src/policies/retrieval.py`
- Create: `tests/test_retrieval.py`

**Step 1: Write the tests**

Create `tests/test_retrieval.py`:

```python
"""Tests for TF-IDF retrieval policy."""

import json
from pathlib import Path

import polars as pl
import pytest

from src.policies.retrieval import RetrievalPolicy


_CORPUS_PATH = Path("tests/fixtures/retrieval_corpus.json")


def _load_corpus() -> dict:
    return json.loads(_CORPUS_PATH.read_text())


def _make_train_data(corpus: dict) -> pl.DataFrame:
    """Build a pseudo-training DataFrame from the corpus for fit()."""
    rows = []
    for doc in corpus["documents"]:
        rows.append({
            "doc_id": doc["id"],
            "title": doc["title"],
            "text": doc["text"],
        })
    return pl.DataFrame(rows)


def test_retrieval_is_base_policy():
    """RetrievalPolicy extends BasePolicy."""
    from src.policies.base import BasePolicy
    assert issubclass(RetrievalPolicy, BasePolicy)


def test_fit_builds_tfidf_matrix():
    """fit() creates a TF-IDF matrix from the corpus."""
    corpus = _load_corpus()
    train = _make_train_data(corpus)
    policy = RetrievalPolicy().fit(train)
    assert policy._tfidf_matrix is not None
    assert policy._tfidf_matrix.shape[0] == len(corpus["documents"])


def test_score_returns_sorted_descending():
    """score() returns (doc_id, score) sorted descending."""
    corpus = _load_corpus()
    train = _make_train_data(corpus)
    policy = RetrievalPolicy().fit(train)

    doc_ids = [d["id"] for d in corpus["documents"]]
    scored = policy.score(doc_ids, context={"query": "machine learning"})

    scores = [s for _, s in scored]
    assert scores == sorted(scores, reverse=True)
    assert len(scored) == len(doc_ids)


def test_score_relevant_docs_rank_higher():
    """Highly relevant documents should rank near the top."""
    corpus = _load_corpus()
    train = _make_train_data(corpus)
    policy = RetrievalPolicy().fit(train)

    doc_ids = [d["id"] for d in corpus["documents"]]

    # Query about gradient boosting — doc 12 should rank high
    scored = policy.score(doc_ids, context={"query": "gradient boosting XGBoost LightGBM"})
    top_5_ids = [doc_id for doc_id, _ in scored[:5]]
    assert 12 in top_5_ids


def test_score_without_query_raises():
    """score() without a query in context raises ValueError."""
    corpus = _load_corpus()
    train = _make_train_data(corpus)
    policy = RetrievalPolicy().fit(train)

    with pytest.raises(ValueError, match="query"):
        policy.score([1, 2, 3], context=None)


def test_evaluate_returns_metrics():
    """evaluate() returns NDCG, MRR, HitRate metrics."""
    corpus = _load_corpus()
    train = _make_train_data(corpus)
    policy = RetrievalPolicy(corpus_path=_CORPUS_PATH).fit(train)
    metrics = policy.evaluate(pl.DataFrame(), k=10)

    assert "ndcg@10" in metrics
    assert "mrr" in metrics
    assert "hit_rate@10" in metrics
    # Scores should be in realistic range, not trivially 0 or 1
    assert 0.3 < metrics["ndcg@10"] < 1.0
    assert 0.3 < metrics["mrr"] < 1.0


def test_unknown_doc_ids_get_zero_score():
    """Documents not in the corpus get score 0."""
    corpus = _load_corpus()
    train = _make_train_data(corpus)
    policy = RetrievalPolicy().fit(train)

    scored = policy.score([1, 9999], context={"query": "machine learning"})
    scores_dict = dict(scored)
    assert scores_dict[9999] == 0.0
    assert scores_dict[1] > 0.0
```

**Step 2: Run tests to verify they fail**

Run: `cd /Users/zenith/Desktop/decide-hub && .venv/bin/python -m pytest tests/test_retrieval.py -v`
Expected: FAIL — `ModuleNotFoundError`

**Step 3: Write the implementation**

Create `src/policies/retrieval.py`:

```python
"""TF-IDF retrieval policy — document ranking by query relevance.

Demonstrates that the BasePolicy interface, evaluation metrics, and CI
regression gates generalize across decision domains. Same harness,
different domain.
"""

import json
from pathlib import Path

import numpy as np
import polars as pl
from sklearn.feature_extraction.text import TfidfVectorizer
from sklearn.metrics.pairwise import cosine_similarity

from src.policies.base import BasePolicy
from src.evaluation.naive import ndcg_at_k, mrr, hit_rate_at_k

_DEFAULT_CORPUS = Path("tests/fixtures/retrieval_corpus.json")


class RetrievalPolicy(BasePolicy):
    """TF-IDF document retrieval with cosine similarity scoring."""

    def __init__(self, corpus_path: Path | None = None):
        self._corpus_path = corpus_path or _DEFAULT_CORPUS
        self._vectorizer: TfidfVectorizer | None = None
        self._tfidf_matrix = None
        self._doc_ids: list[int] = []
        self._doc_id_to_idx: dict[int, int] = {}

    def fit(self, train_data: pl.DataFrame) -> "RetrievalPolicy":
        """Build TF-IDF matrix from document corpus.

        Args:
            train_data: DataFrame with doc_id, title, text columns.
                        Used for the TF-IDF vocabulary.
        """
        doc_ids = train_data["doc_id"].to_list()
        titles = train_data["title"].to_list()
        texts = train_data["text"].to_list()

        # Combine title and text for richer TF-IDF representation
        combined = [f"{t} {txt}" for t, txt in zip(titles, texts)]

        self._vectorizer = TfidfVectorizer(stop_words="english")
        self._tfidf_matrix = self._vectorizer.fit_transform(combined)
        self._doc_ids = doc_ids
        self._doc_id_to_idx = {did: i for i, did in enumerate(doc_ids)}
        return self

    def score(
        self, items: list[int], context: dict | None = None,
    ) -> list[tuple[int, float]]:
        """Rank documents by cosine similarity to query.

        Args:
            items: Document IDs to rank.
            context: Must contain {"query": str}.

        Returns:
            List of (doc_id, similarity_score) sorted descending.
        """
        if not context or "query" not in context:
            raise ValueError("context must contain 'query' key for retrieval")

        query = context["query"]
        query_vec = self._vectorizer.transform([query])
        sim = cosine_similarity(query_vec, self._tfidf_matrix).flatten()

        scored = []
        for doc_id in items:
            idx = self._doc_id_to_idx.get(doc_id)
            score = float(sim[idx]) if idx is not None else 0.0
            scored.append((doc_id, score))
        scored.sort(key=lambda x: x[1], reverse=True)
        return scored

    def evaluate(self, test_data: pl.DataFrame, k: int = 10) -> dict[str, float]:
        """Evaluate retrieval quality using corpus queries with graded relevance.

        Ignores test_data — uses the queries and relevance judgments from
        the corpus fixture. This is standard for retrieval evaluation
        (queries are the test set, not user interactions).
        """
        corpus = json.loads(self._corpus_path.read_text())
        queries = corpus["queries"]
        all_doc_ids = [d["id"] for d in corpus["documents"]]

        ndcg_scores = []
        mrr_scores = []
        hit_scores = []

        for q in queries:
            relevant = set(int(doc_id) for doc_id in q["relevant"])
            ranked = self.score(all_doc_ids, context={"query": q["text"]})
            ranked_ids = [doc_id for doc_id, _ in ranked]

            ndcg_scores.append(ndcg_at_k(ranked_ids, relevant, k))
            mrr_scores.append(mrr(ranked_ids, relevant))
            hit_scores.append(hit_rate_at_k(ranked_ids, relevant, k))

        return {
            f"ndcg@{k}": sum(ndcg_scores) / len(ndcg_scores),
            "mrr": sum(mrr_scores) / len(mrr_scores),
            f"hit_rate@{k}": sum(hit_scores) / len(hit_scores),
        }
```

**Step 4: Run tests to verify they pass**

Run: `cd /Users/zenith/Desktop/decide-hub && .venv/bin/python -m pytest tests/test_retrieval.py -v`
Expected: All 8 tests PASS

**Step 5: Commit**

```bash
git add src/policies/retrieval.py tests/test_retrieval.py
git commit -m "feat: add TF-IDF retrieval policy with graded relevance evaluation"
```

---

## Task 3: Serving Integration

Register retrieval policy in the API.

**Files:**
- Modify: `src/serving/schemas.py:14` (add "retrieval" to policy pattern)
- Modify: `src/serving/app.py` (register retrieval in lifespan, add corpus loading)

**Step 1: Update schema**

Modify `src/serving/schemas.py` — update policy pattern:

```python
    policy: str = Field(default="popularity", pattern="^(popularity|scorer|bandit|retrieval)$")
```

**Step 2: Register retrieval in app.py lifespan**

Add import after the bandit import:

```python
from src.policies.retrieval import RetrievalPolicy
```

Add corpus loading and policy registration after the bandit block in lifespan:

```python
    try:
        import json
        from pathlib import Path
        corpus_path = Path("tests/fixtures/retrieval_corpus.json")
        if corpus_path.exists():
            corpus_data = json.loads(corpus_path.read_text())
            import polars as pl
            doc_rows = [
                {"doc_id": d["id"], "title": d["title"], "text": d["text"]}
                for d in corpus_data["documents"]
            ]
            retrieval = RetrievalPolicy(corpus_path=corpus_path).fit(pl.DataFrame(doc_rows))
            _policies["retrieval"] = retrieval
    except Exception as e:
        print(f"Warning: RetrievalPolicy failed to fit: {e}")
```

Update the candidate items fallback in `/rank` — add after `_all_items` check:

```python
    elif hasattr(policy, "_doc_ids"):
        candidates = policy._doc_ids
```

**Step 3: Update /rank to pass query context for retrieval**

The current `/rank` endpoint passes `context={"user_id": req.user_id}` which doesn't include a query. Add an optional `query` field to `RankRequest`:

In `src/serving/schemas.py`, add to `RankRequest`:

```python
    query: str | None = Field(default=None, description="Query string for retrieval policies")
```

In `src/serving/app.py`, update context building in the `/rank` handler:

```python
    ctx = {"user_id": req.user_id}
    if req.query:
        ctx["query"] = req.query
    scored = policy.score(candidates, context=ctx)
```

**Step 4: Add serving test**

Append to `tests/test_serving.py`:

```python
def test_rank_retrieval_policy():
    """Retrieval policy returns ranked documents via /rank endpoint."""
    with TestClient(app) as client:
        resp = client.post("/rank", json={
            "user_id": 0,
            "policy": "retrieval",
            "query": "machine learning",
            "k": 5,
        })
        if resp.status_code == 200:
            data = resp.json()
            assert data["policy"] == "retrieval"
            assert len(data["items"]) == 5
        else:
            assert resp.status_code == 404
```

**Step 5: Run tests**

Run: `cd /Users/zenith/Desktop/decide-hub && .venv/bin/python -m pytest tests/test_serving.py tests/test_retrieval.py -v`
Expected: All PASS

**Step 6: Commit**

```bash
git add src/serving/schemas.py src/serving/app.py tests/test_serving.py
git commit -m "feat: register retrieval policy in /rank endpoint with query support"
```

---

## Task 4: README + DECISIONS.md

**Files:**
- Modify: `README.md` (add retrieval benchmarks section)
- Modify: `DECISIONS.md` (append entry)

**Step 1: Run evaluation and note scores**

Run: `cd /Users/zenith/Desktop/decide-hub && .venv/bin/python -c "
import json, polars as pl
from pathlib import Path
from src.policies.retrieval import RetrievalPolicy
corpus_path = Path('tests/fixtures/retrieval_corpus.json')
corpus = json.loads(corpus_path.read_text())
docs = pl.DataFrame([{'doc_id': d['id'], 'title': d['title'], 'text': d['text']} for d in corpus['documents']])
policy = RetrievalPolicy(corpus_path=corpus_path).fit(docs)
metrics = policy.evaluate(pl.DataFrame(), k=10)
for k, v in metrics.items():
    print(f'{k}: {v:.4f}')
"`

**Step 2: Add retrieval benchmarks to README**

Add after the bandit comparison section:

```markdown
## Retrieval Benchmarks (Synthetic Corpus)

| Policy | NDCG@10 | MRR | HitRate@10 |
|--------|---------|-----|------------|
| TF-IDF Retrieval | _from output_ | _from output_ | _from output_ |

30-document corpus with 12 queries and graded relevance judgments. Same
BasePolicy interface, same evaluation metrics, same CI gates — different
decision domain. See [DECISIONS.md](DECISIONS.md) for the "same harness,
different domain" rationale.
```

**Step 3: Add DECISIONS.md entry**

Append to `DECISIONS.md`:

```markdown

## 15. TF-IDF retrieval over FAISS for a 30-document corpus

The retrieval policy uses scikit-learn's TfidfVectorizer + cosine
similarity, not FAISS or BM25. Adding a vector index dependency to
rank 30 documents would be engineering theater. The portfolio story is
"same harness, different domain" — the retrieval implementation is the
least important part. What matters is that the same BasePolicy interface,
NDCG/MRR/HitRate evaluation, and CI regression gates apply to document
retrieval identically to item ranking.

The corpus uses graded relevance (3/2/1) with deliberate vocabulary
overlap between documents. This produces realistic NDCG@10 scores in
the 0.6-0.9 range rather than trivial 1.0. A regression gate can
detect actual degradation in this range.
```

**Step 4: Commit**

```bash
git add README.md DECISIONS.md
git commit -m "docs: add retrieval benchmarks and DECISIONS.md entry"
```

---

## Post-Task Checklist

1. **Run full test suite:** `make test` — all tests pass
2. **Verify /rank retrieval:** `curl -X POST http://localhost:8000/rank -H "Content-Type: application/json" -d '{"user_id": 0, "policy": "retrieval", "query": "machine learning", "k": 5}'`
3. **Fill README values:** Update benchmark table with actual script output
